function onCreatePost()
    
        if (getPropertyFromClass('ClientPrefs', 'funni')) == true then
    
      --[[      for i = 0,16 do
                setPropertyFromGroup('notes',i,'scale.x',100)
                setPropertyFromGroup('strumLineNotes',i,'scale.x',100)
            end
            ]]
            for e = 0,16 do
                setPropertyFromGroup('notes',e,'scale.x',100)
            end

        end

    
    
    
    
    
        
    
    end